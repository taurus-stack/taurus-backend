[pytest]
python_files = test_*.py tests/**/*.py
testpaths = tests
DJANGO_SETTINGS_MODULE = application.settings
markers =
    compliance: ExecutableUnit adapter 合规测试。用 `pytest -m compliance` 仅执行合规验证。
    slow: 端到端/长耗时测试。默认执行，CI 可通过 `-m "not slow"` 跳过。
    perf: 性能压测基线。用 `pytest -m perf` 仅执行性能测试。